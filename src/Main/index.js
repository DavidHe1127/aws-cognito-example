import React from 'react';
import {Auth} from 'aws-amplify';

class Main extends React.Component {
  render() {
    return (
      <div className="container">
        <h1>Congras you're now logged in and made the first authed api!!!</h1>
      </div>
    );
  }
}

export default Main;
